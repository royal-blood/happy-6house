spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/class6?serverTimezone=UTC
    username: ssafy
    password: ssafy
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  config:
    import:
      application-default-oauth.yml